echo "JOBS               = 4"                        > Makefile.local
echo "CMAKE_GENERATOR    = Ninja"                   >> Makefile.local
echo "CMAKE_FLAGS_SLLVM += -DLLVM_BUILD_LLVM_DYLIB" >> Makefile.local
make fetch
make configure
make install
make build-vulcan
make test
